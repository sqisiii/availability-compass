<UserControl x:Class="AvailabilityCompass.WpfClient.Pages.ManageCalendars.Dialogs.BaseRecurringDateCrudView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Margin="10">
        <TextBlock
            Margin="0,10,0,20"
            HorizontalAlignment="Center"
            Style="{StaticResource MaterialDesignHeadline6TextBlock}"
            Text="{Binding ViewHeaderText, ElementName=Root }" />
        <TextBox
            Width="200"
            Margin="0,0,0,10"
            HorizontalAlignment="Left"
            IsEnabled="{Binding IsViewEnabled, ElementName=Root}"
            materialDesign:HintAssist.Hint="Description"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding SelectedItem.Description, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"
            ToolTip="Short description of the selected recurring days" />

        <DatePicker
            Width="120"
            Margin="0,0,0,15"
            HorizontalAlignment="Left"
            IsEnabled="{Binding IsViewEnabled, ElementName=Root}"
            materialDesign:HintAssist.HelperText="Format: yyyy-MM-dd"
            materialDesign:HintAssist.Hint="Start Date"
            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
            Text="{Binding SelectedItem.StartDateString, ValidatesOnDataErrors=True, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, StringFormat=yyyy-MM-dd}" />

        <TextBox
            Width="200"
            Margin="0,0,0,15"
            HorizontalAlignment="Left"
            IsEnabled="{Binding IsViewEnabled, ElementName=Root}"
            materialDesign:HintAssist.Hint="Duration"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding SelectedItem.Duration, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"
            ToolTip="The number of marked days" />

        <TextBox
            Width="200"
            Margin="0,0,0,15"
            HorizontalAlignment="Left"
            IsEnabled="{Binding IsViewEnabled, ElementName=Root}"
            materialDesign:HintAssist.Hint="Frequency"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding SelectedItem.RepetitionPeriod, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"
            ToolTip="The number of days " />

        <TextBox
            Width="200"
            Margin="0,0,0,15"
            HorizontalAlignment="Left"
            IsEnabled="{Binding IsViewEnabled, ElementName=Root}"
            materialDesign:HintAssist.Hint="Number of Repetitions"
            Style="{StaticResource MaterialDesignFloatingHintTextBox}"
            Text="{Binding SelectedItem.NumberOfRepetitions, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, ValidatesOnNotifyDataErrors=True}"
            ToolTip="Number of the repetitions of the sequence" />

        <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
            <Button
                Margin="0,8,8,0"
                Command="{Binding SaveCommand}"
                Content="{Binding SaveCommandPrompt, ElementName=Root}"
                IsDefault="True"
                Style="{StaticResource MaterialDesignFlatButton}">
                <Button.CommandParameter>
                    <system:Boolean xmlns:system="clr-namespace:System;assembly=mscorlib">
                        True
                    </system:Boolean>
                </Button.CommandParameter>
            </Button>
            <Button
                Margin="0,8,8,0"
                Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                Content="CANCEL"
                IsCancel="True"
                Style="{StaticResource MaterialDesignFlatButton}">
                <Button.CommandParameter>
                    <system:Boolean xmlns:system="clr-namespace:System;assembly=mscorlib">
                        False
                    </system:Boolean>
                </Button.CommandParameter>
            </Button>
        </StackPanel>

    </StackPanel>
</UserControl>