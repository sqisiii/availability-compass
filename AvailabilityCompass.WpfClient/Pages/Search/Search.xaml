<UserControl
    x:Class="AvailabilityCompass.WpfClient.Pages.Search"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="clr-namespace:AvailabilityCompass.WpfClient.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:AvailabilityCompass.Core.Features.Search;assembly=AvailabilityCompass.Core"
    d:DataContext="{d:DesignInstance viewModels:SearchViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="300"
    d:DesignWidth="600"
    mc:Ignorable="d">
    <StackPanel>
        <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="Select Sources:" />

        <WrapPanel Margin="0,20,0,0" Orientation="Horizontal">
            <ListBox ItemsSource="{Binding Sources}">
                <ListBox.ItemContainerStyle>
                    <!--  Style to get rid of the selection visual  -->
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Margin" Value="5,0,5,5" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:SourceViewModel}">
                        <StackPanel>
                            <components:SelectionCheckBox
                                CheckBoxText="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                FullMinHeight="30"
                                FullWidth="180"
                                HeaderText="{Binding LastUpdated}"
                                IsActive="{Binding IsActive, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </WrapPanel>

        <TextBlock Margin="0,10,0,0" Style="{StaticResource MaterialDesignHeadline6TextBlock}" Text="Select Calendars:" />

        <WrapPanel Margin="0,10,0,0" Orientation="Horizontal">
            <ListBox ItemsSource="{Binding Calendars}">
                <ListBox.ItemContainerStyle>
                    <!--  Style to get rid of the selection visual  -->
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Margin" Value="5,0,5,5" />
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:CalendarViewModel}">
                        <StackPanel>
                            <components:SelectionCheckBox
                                CheckBoxText="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                FullMinHeight="30"
                                FullWidth="150"
                                IsActive="True"
                                HeaderText="{Binding Type}"
                                IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </WrapPanel>
    </StackPanel>
</UserControl>